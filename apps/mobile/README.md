# Vexoryl Mobile

React Native starter boundary for the mobile viewer and streamer experiences. The web/API contracts are intentionally shared: use `/api/streams/:id/connect` for presence, Firebase Cloud Messaging for notifications, and a Firebase-backed listener for chat in the native client. Follow platform-native navigation and Material/HIG controls when this app is bootstrapped with Expo or React Native CLI.
